version: '3'
services:
  app:
    image: node:10.15.3-jessie
    working_dir: /srv
    environment:
      - TZ=Asia/Tokyo
    volumes:
      - .:/app
      - node_modules:/srv/node_modules
    tty: true
    ports:
      - 8000:8000

volumes:
    node_modules: {}